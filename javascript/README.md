# Node.js Javascript Template

## Requirements

* Node.js >= 16

## Setup

To install all dependencies run:

```bash
npm install
```

Build (clean, lint, and transpile):

```bash
npm run build
```

Run tests:

```bash
npm run test
```
